哈希表：
1.哈希表的作用是快速判断一个元素是否出现在集合里
2.对于有限总数，可以定义一个数组进行统计。比如英文字母只有26个。见242
3.对于无限总数统计单一值，可以使用unordered_set实现。unordered_set的初始化可以直接借助数组：unordered_set<int> nums(nums1.begin(), nums1.end()); 对于unordered_set的使用结合find和end函数作比较进行搜索。见349
4.对于存在多个值匹配，既有权值也有键值。使用map这种结构。见1
5.对于多数求和这一类问题，选择指定一个，去寻找剩下的是否存在差值。见1
6.对于求四数之和，两两分类。两重for循环统计ab所有和可能值，再两重for循环统计目标值减另外两个和出现的次数(find和end结合)。见454
7.多数求和有两种类型：
（1）集合中没有相同元素，不可以重复使用集合元素。方法：多重for循环+map
（2）集合中有相同元素，但结果中不能有重复。方法：多重for循环(n-2)+left/right双指针+去重。见15/18
8.有left和right的情况，在判断条件(if/while)时。在while内必须要要加left<right，普通if不需要加
