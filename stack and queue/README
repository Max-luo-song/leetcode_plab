栈与队列：
1.两个栈可以模拟队列。见232。两个队列可以模拟栈。但后者顺序不变
2.对于匹配问题，数据结构使用栈来完成
3.string类型的数字转化十进制，可以使用stoi函数。见150
4.逆波兰式计算时，应该弄清除数和被除数，减数和被减数。见150
5.对于固定窗口内求最大值，结合滑动窗口思想+队列结构，形成单调队列进行计算。见239
6.对于前K个高频元素，可以使用数值大小为K的小顶堆实现。借助于优先级队列。见347
